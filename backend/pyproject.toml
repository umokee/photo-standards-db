[project]
name = "backend"
version = "0.1.0"
requires-python = "==3.13"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "python-multipart",

  "sqlalchemy",
  "asyncpg",
  "pydantic-settings",
  "pydantic",

  "bcrypt",

  "opencv-python",
]

[project.scripts]
backend = "src.main:main"


