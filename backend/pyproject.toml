[project]
name = "backend"
version = "0.1.0"
requires-python = "==3.13"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "python-multipart",

  "sqlalchemy",
  "asyncpg",

  "bcrypt",

  "pydantic-settings",
  "pydantic",
]

[project.scripts]
backend = "src.main:main"


